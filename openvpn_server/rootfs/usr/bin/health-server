#!/usr/bin/with-contenv bashio
# shellcheck shell=bash
# Simple HTTP health check server for watchdog

while true; do
    echo -e "HTTP/1.1 200 OK\n\n{\"status\":\"healthy\"}" | nc -l -p 8099 -q 1
done
